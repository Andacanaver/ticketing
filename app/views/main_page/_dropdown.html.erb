<%= form_with model: user do |form| %>
<div>
    <%= form.label :role %><br>
    <%= form.select :role, options_for_select(User.roles.map {|key, value| [key.titleize, User.roles.key(value)]}, @user.role) %><br>
    <% user.errors.full_messages_for(:role).each do |message| %>
      <div><%= message %></div>
    <% end %>
</div>
<% end %>